name: example
description: An example app.
version: 1.0.0+1
environment:
  sdk: '>=2.1.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  animation_player:
    path: ../
  cupertino_icons:
  progress_animation_builder:
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
